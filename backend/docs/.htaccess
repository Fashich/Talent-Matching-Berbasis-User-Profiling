# Blok akses langsung lewat URL browser — folder ini isinya dokumen
# internal (ERD/Class Diagram .mmd & .png), bukan buat diakses publik.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
